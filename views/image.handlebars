{{!-- {{#each image}}
<img src="/images1/{{this}}" alt=""> {{/each}} --}}
<h4>If the image not reloaded properly...reload this page</h4>
<div class="row justify-content-center">
    {{#each image}}
    <div class="col-md-4">
        <img src="/images/{{this}}" class="img-fluid">
    </div>
    {{/each}}
    
</div>